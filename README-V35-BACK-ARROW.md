# رحّال V35 — زر رجوع بأسلوب التطبيقات

- تم تحويل زر الرجوع في الصفحات الداخلية إلى سهم `←` فقط.
- تم الحفاظ على `aria-label` و`title` كـ «رجوع» عند وجود زر رجوع.
- تم تصغير الزر ليظهر كعنصر App Bar بسيط.
- الصفحة الرئيسية لا تحتاج زر رجوع.
- تم الاعتماد على نسخة V34-login-fixed كأساس.
